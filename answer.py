class Answer:
	def __init__(self, corpus):
		self.corpus = corpus

	def answer(self, question_or_fact_AMR_graph):
		question_or_fact_AMR_graph.show()
		response = "\nSome magic will happen here but not for now..."
		return(response)